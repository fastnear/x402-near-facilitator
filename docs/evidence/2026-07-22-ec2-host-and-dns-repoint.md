# EC2 host provisioning and DNS repoint evidence — 2026-07-22

Owner: Mike Purvis

This record covers provisioning of the dedicated personal launch host and
the repoint of both public DNS records to it. It is not evidence that TLS,
Nginx, PostgreSQL, either facilitator service, a release, or any funded flow
is live. It supersedes the target recorded in the same-day
[launch DNS evidence](2026-07-22-launch-dns.md): the shared-host plan was
retired in favor of solo, personally funded infrastructure.

## Host

- AWS EC2 `i-0537770b34b04b820`, name `x402-facilitator`, `t3.small`,
  `us-west-2a`, account `341982967115`.
- AMI `ami-0ac74609c6396bed3`
  (`ubuntu/images/hvm-ssd-gp3/ubuntu-noble-24.04-amd64-server-20260714`,
  Canonical owner `099720109477`).
- Elastic IP `100.23.147.163` (`eipalloc-0fb350dd6954a63ca`), stable across
  stop/start.
- Root volume: 30 GiB gp3, encrypted with the account default EBS key,
  delete-on-termination.
- IMDSv2 required (`HttpTokens=required`).
- Security group `sg-015857c4ae084d397`: tcp/443 and tcp/80 from
  `0.0.0.0/0` and `::/0`; tcp/22 only from the operator workstation
  address; default egress.
- Key pair `mike-ed25519` imported from the operator workstation's existing
  ED25519 public key; no private key material was generated by or stored in
  AWS.
- SSH-verified baseline: `x86_64`, Ubuntu 24.04.4 LTS, glibc 2.39,
  systemd 255, 2 vCPU, 2 GiB memory, 29 GiB root filesystem. This satisfies
  the release build target `x86_64-unknown-linux-gnu`.

## DNS repoint

- Hosted zone `mikedotexe.com.` (`ZEBBWSGTKUUP6`), Route 53 change
  `C082360123PQRZEZ9MDOK`, `INSYNC` the same day.
- Previewed and explicitly confirmed batch, `UPSERT`, TTL 300:
  - `x402.mikedotexe.com.` `A` `65.108.111.182` → `100.23.147.163`
  - `test.x402.mikedotexe.com.` `A` `65.108.111.182` → `100.23.147.163`
- Verified after `INSYNC`: authoritative `ns-799.awsdns-35.net`, `1.1.1.1`,
  and `8.8.8.8` all answer `100.23.147.163` for both names.
- No other record in the zone was touched. AAAA remains deferred; the
  instance has no IPv6 address.

## Credential scope

All changes were made from the operator workstation with the `for-easy-dns`
IAM user, which now holds the managed `AmazonEC2FullAccess` policy (attached
by the owner on 2026-07-22) in addition to the zone-scoped Route 53 inline
policy. No AWS credential exists on the launch host, and the instance has no
IAM role.

## Remaining launch gates

The host is otherwise untouched: no OS updates applied, no Nginx,
PostgreSQL, system users, TLS certificate, or release installed, and neither
hostname serves HTTPS as of this evidence date. Those gates remain unchecked
in the launch checklist.
